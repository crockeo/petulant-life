(ns petulant-life.config)

;; Globally defining the window width and height.
(def window-width 640)
(def window-height 480)

(def window-size {:width window-width
                  :height window-height})
